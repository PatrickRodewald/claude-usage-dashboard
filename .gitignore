# Windows-Verknuepfungen betten den Rechnernamen und Volume-GUIDs ein.
# Die .lnk wird lokal erzeugt (siehe README), nicht eingecheckt.
*.lnk

node_modules/
npm-debug.log*

# Persoenliche Anpassungen, ohne die Vorlage zu ueberschreiben
config.local.json

# Das Verbrauchsarchiv ist persoenlich: Projektnamen, Zeiten, Tokenmengen.
# Gehoert auf den eigenen Rechner, nicht in ein oeffentliches Repository.
data/

.DS_Store
Thumbs.db
.idea/
.vscode/
